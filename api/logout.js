const {json,method,requireSameOrigin}=require('../lib/http');const {clearSession}=require('../lib/auth');module.exports=async(req,res)=>{if(!method(req,res,['POST'])||!requireSameOrigin(req,res))return;clearSession(res);return json(res,200,{ok:true});};
