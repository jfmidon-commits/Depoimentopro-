const {TABLES}=require('../lib/config');const air=require('../lib/airtable');const {json,method}=require('../lib/http');const validate=require('../lib/validate');const {siteKey}=require('../lib/turnstile');async function findActive(token){const t=air.escapeFormula(token);return air.findOne(TABLES.campaigns,`AND({Public Token}='${t}',{Status}='Ativa')`);}module.exports=async(req,res)=>{if(!method(req,res,['GET']))return;try{let token;try{token=validate.publicToken(req.query?.token,'Link');}catch(e){return json(res,400,{error:e.message});}const campaign=await findActive(token);if(!campaign)return json(res,404,{error:'Campanha não encontrada ou pausada.'});const userId=campaign.fields?.['User']?.[0];if(!userId)return json(res,404,{error:'Campanha inválida.'});const user=await air.get(TABLES.users,userId);const f=user.fields||{};const plan=f['Plano']||'Free';const linked=Array.isArray(f['Testimonials'])?f['Testimonials'].length:0;const used=Math.max(Number(f['Depoimentos Usados']||0),linked);const limit=Number(f['Limite Depoimentos']||0);if(plan!=='Pro'&&used>=limit)return json(res,403,{error:'Esta campanha atingiu o limite de depoimentos do plano atual.'});return json(res,200,{campaign:{nome:campaign.fields?.['Nome']||'Enviar depoimento',mensagem:'Conte de forma sincera como foi sua experiência.'},turnstileSiteKey:siteKey()});}catch(e){console.error('public-campaign',e);return json(res,500,{error:'Não foi possível carregar esta campanha.'});}};
