const {TABLES}=require('../lib/config');const air=require('../lib/airtable');const {bodyJson,json,method,clientIp}=require('../lib/http');const {allowRate,rateKey}=require('../lib/security');const validate=require('../lib/validate');const {verifyTurnstile}=require('../lib/turnstile');const sleep=ms=>new Promise(r=>setTimeout(r,ms));function testimonialOrder(a,b){const da=String(a.fields?.['Data Criacao']||a.createdTime||'');const db=String(b.fields?.['Data Criacao']||b.createdTime||'');return da.localeCompare(db)||a.id.localeCompare(b.id);}module.exports=async(req,res)=>{if(!method(req,res,['POST']))return;try{const ip=clientIp(req);if(!allowRate(rateKey('testimonial',ip),{limit:6,windowMs:60_000})){res.setHeader('Retry-After','60');return json(res,429,{error:'Muitas tentativas. Tente novamente em instantes.'});}let input;try{input=validate.testimonialInput(await bodyJson(req));}catch(e){if(e instanceof validate.ValidationError)return json(res,e.statusCode,{error:e.message});throw e;}if(input.website)return json(res,200,{ok:true});if(!input.consentimento)return json(res,400,{error:'É necessário autorizar a revisão e eventual publicação.'});const turnstile=await verifyTurnstile(input.turnstileToken,ip);if(!turnstile.ok)return json(res,400,{error:turnstile.error});const t=air.escapeFormula(input.token);const campaign=await air.findOne(TABLES.campaigns,`AND({Public Token}='${t}',{Status}='Ativa')`);if(!campaign)return json(res,404,{error:'Campanha não encontrada ou pausada.'});const userId=campaign.fields?.['User']?.[0];if(!userId)return json(res,400,{error:'Campanha inválida.'});const user=await air.get(TABLES.users,userId);const uf=user.fields||{};const plan=uf['Plano']||'Free';const limit=Number(uf['Limite Depoimentos']||0);const linkedBefore=Array.isArray(uf['Testimonials'])?uf['Testimonials']:[];const used=Math.max(Number(uf['Depoimentos Usados']||0),linkedBefore.length);if(plan!=='Pro'&&used>=limit)return json(res,403,{error:'Esta campanha atingiu o limite do plano atual.'});const record=await air.create(TABLES.testimonials,{'Nome Cliente':input.nomeCliente,'Texto':input.texto,'Nota':input.nota,'Status':'Pendente','Origem':'Form','Data Criacao':new Date().toISOString(),'User':[userId],'Campaign':[campaign.id],'Consentimento Publicacao':true});let finalUsed=used+1;if(plan!=='Pro'){await sleep(200);const refreshedUser=await air.get(TABLES.users,userId);const linked=Array.isArray(refreshedUser.fields?.['Testimonials'])?refreshedUser.fields['Testimonials']:[];const records=await air.getMany(TABLES.testimonials,linked,100);records.sort(testimonialOrder);const allowed=new Set(records.slice(0,limit).map(r=>r.id));if(!allowed.has(record.id)){await air.remove(TABLES.testimonials,record.id);return json(res,409,{error:'O limite do plano foi atingido por outro envio simultâneo.'});}finalUsed=Math.min(linked.length,limit);}else{const refreshedUser=await air.get(TABLES.users,userId);const linked=Array.isArray(refreshedUser.fields?.['Testimonials'])?refreshedUser.fields['Testimonials']:[];finalUsed=Math.max(finalUsed,linked.length);}const refreshedCampaign=await air.get(TABLES.campaigns,campaign.id);const linkedResponses=Array.isArray(refreshedCampaign.fields?.['Testimonials'])?refreshedCampaign.fields['Testimonials'].length:Number(campaign.fields?.['Total Respostas']||0)+1;await Promise.all([air.update(TABLES.users,userId,{'Depoimentos Usados':finalUsed}),air.update(TABLES.campaigns,campaign.id,{'Total Respostas':linkedResponses})]);return json(res,201,{ok:true,id:record.id});}catch(e){console.error('testimonials',e);return json(res,500,{error:'Não foi possível enviar o depoimento.'});}};
