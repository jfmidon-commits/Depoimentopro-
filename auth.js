function msg(el, text, ok=false){el.textContent=text;el.className='message '+(ok?'success':'error')}
async function post(url, data){const r=await fetch(url,{method:'POST',headers:{'Content-Type':'application/json'},body:JSON.stringify(data)});const j=await r.json().catch(()=>({}));if(!r.ok)throw new Error(j.error||'Erro inesperado');return j}
const signup=document.querySelector('#signup-form');if(signup)signup.addEventListener('submit',async e=>{e.preventDefault();const m=document.querySelector('#msg');msg(m,'Criando conta…',true);const fd=new FormData(signup);try{await post('/api/signup',{nome:fd.get('nome'),email:fd.get('email'),password:fd.get('password')});location.href='/dashboard'}catch(err){msg(m,err.message)}});
const login=document.querySelector('#login-form');if(login)login.addEventListener('submit',async e=>{e.preventDefault();const m=document.querySelector('#msg');msg(m,'Entrando…',true);const fd=new FormData(login);try{await post('/api/login',{email:fd.get('email'),password:fd.get('password')});location.href='/dashboard'}catch(err){msg(m,err.message)}});
