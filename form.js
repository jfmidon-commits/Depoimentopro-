const token=new URLSearchParams(location.search).get('token')||'';const form=document.querySelector('#testimonial-form');const err=document.querySelector('#load-error');function msg(el,t,ok=false){el.textContent=t;el.className='message '+(ok?'success':'error')}async function jsonFetch(url,opt={}){const r=await fetch(url,{...opt,headers:{'Content-Type':'application/json',...(opt.headers||{})}});const j=await r.json().catch(()=>({}));if(!r.ok)throw new Error(j.error||'Erro inesperado');return j}async function init(){try{const d=await jsonFetch('/api/public-campaign?token='+encodeURIComponent(token));document.querySelector('#campaign-title').textContent=d.campaign.nome;document.querySelector('#campaign-message').textContent=d.campaign.mensagem||'';form.hidden=false}catch(e){document.querySelector('#campaign-title').textContent='Não foi possível abrir este formulário';msg(err,e.message)}}form.addEventListener('submit',async e=>{e.preventDefault();const m=document.querySelector('#form-msg');const fd=new FormData(form);msg(m,'Enviando…',true);try{await jsonFetch('/api/testimonials',{method:'POST',body:JSON.stringify({token,nomeCliente:fd.get('nomeCliente'),texto:fd.get('texto'),nota:Number(fd.get('nota')),consentimento:document.querySelector('#consentimento').checked,website:fd.get('website')})});form.reset();form.querySelector('button[type=submit]').disabled=true;msg(m,'Obrigado! Seu depoimento foi enviado e ficará pendente até a moderação.',true)}catch(e){msg(m,e.message)}});init();
