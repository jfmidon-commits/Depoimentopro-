function siteKey(){if(!process.env.TURNSTILE_SECRET_KEY)return '';return String(process.env.TURNSTILE_SITE_KEY||'').trim();}
async function verifyTurnstile(responseToken,remoteIp){const secret=String(process.env.TURNSTILE_SECRET_KEY||'').trim();if(!secret)return{ok:true,skipped:true};if(!responseToken)return{ok:false,error:'Confirme que você não é um robô.'};try{const body=new URLSearchParams({secret,response:responseToken});if(remoteIp&&remoteIp!=='unknown')body.set('remoteip',remoteIp);const response=await fetch('https://challenges.cloudflare.com/turnstile/v0/siteverify',{method:'POST',headers:{'Content-Type':'application/x-www-form-urlencoded'},body});const data=await response.json().catch(()=>({}));return data.success?{ok:true}:{ok:false,error:'Não foi possível validar o desafio anti-spam.'};}catch(error){console.error('turnstile',error);return{ok:false,error:'Não foi possível validar o desafio anti-spam.'};}}
module.exports={siteKey,verifyTurnstile};
